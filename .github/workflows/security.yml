name: security
on: [pull_request]
jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: gitleaks / trufflehog + bağımlılık lisans envanteri (PKE-Plan STD-04, E-05, E-08)
      - run: echo "secret scan placeholder"
