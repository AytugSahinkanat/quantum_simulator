# Profil 3 (Demo/Üretim) — tek dış giriş noktası, TLS termination.
# HLD 3.4.1: web istemcisi ve proxy QuanT ağına doğrudan erişemez.
server {
    listen 443 ssl;
    location /api/ { proxy_pass http://api:8000/; }
    location / { proxy_pass http://web:5173/; }
}
